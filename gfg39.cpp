#include<iostream>
using namespace std;
struct node
{
	int data;
	node*next;
};
typedef node* lptr;
lptr create(int);
lptr insert(lptr,int);
void print(lptr);
int main()
{
	int n,x,i;
	lptr h1=NULL,h2=NULL,h3=NULL,t=NULL;
	cout<<
}